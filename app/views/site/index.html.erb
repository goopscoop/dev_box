<nav>
  <div class="nav-wrapper">
    <a href="/" class="brand-logo">{ DevBox }</a>
    <a href="#" data-activates="mobile_sidebar" class="button-collapse"><i class="mdi-navigation-menu"></i></a>
    <ul class="right hide-on-med-and-down">
      <li><a href="/about">About</a></li>
      <li ng-show="currentUser">{{currentUser.firstName || 'User'}}</li>
      <li>
        <a href="#" ng-click="showLogin($event)" ng-hide="currentUser">Login</a>
        <a href="#" ng-click="logout()" ng-show="currentUser">Logout</a>
      </li>
    </ul>
    <ul class="side-nav" id="mobile_sidebar">
      <li><a href="/about">About</a></li>
      <li ng-show="currentUser">{{currentUser.firstName || 'User'}}</li>
      <li>
        <a href="#" ng-click="showLogin($event)" ng-hide="currentUser">Login</a>
        <a href="#" ng-click="logout()" ng-show="currentUser">Logout</a>
      </li>
    </ul>
  </div>
</nav>

<div class="container">
  <alerts></alerts>

<h1>Welcome to my awesome site!!!</h1>

<% if @current_user %>
  Hello, <%= @current_user.name %> | <a href="/auth/logout">Logout</a>
<% else %>
  <a href="/auth/github">Login with GitHub</a>
  <a href="/auth/stackexchange">Login with StackOverflow</a>
<% end %>

<hr>
<%= @current_user.inspect %>


<ng-view></ng-view>
